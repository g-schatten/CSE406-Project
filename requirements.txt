# Runtime: standard library only (hashlib, hmac, struct, argparse, itertools).
# No third-party dependencies are required to run wpacrack.

# Development / testing only:
pytest>=7.0

# Optional, only for sim/plot.py charts (not required by the attacks):
# matplotlib>=3.5
